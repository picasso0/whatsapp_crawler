version: '3'
services:
  worker:
    container_name: worker
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    environment:
      - TOKEN=test
      - C2_URL=http://127.0.0.1:8888